
  # Convert design to code

  This is a code bundle for Convert design to code. The original project is available at https://www.figma.com/design/EMCQy1eSTA1xv6UDiMKszB/Convert-design-to-code.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  